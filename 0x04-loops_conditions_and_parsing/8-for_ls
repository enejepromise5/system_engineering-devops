#!/usr/bin/env bash
# This script lists non-hidden files in the current directory and displays only the part after the first dash in each filename

for file in *; do
  # Skip if there's no dash
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  else
    echo "$file"
  fi
done
